---
/**
 * Awards — About 섹션 내부에 노출되는 수상 카드 그리드.
 * - awards.ts에서 데이터 로드, 2×2 grid로 노출
 * - rankKey에 따라 트로피·메달 이모지와 액센트 색상 분기
 * - 'TS' 같은 팀명은 카드 우상단 칩으로 강조
 */
import { awards } from '../data/awards';
---

<section id="awards" class="py-16 md:py-24 relative">
  <div class="max-w-7xl mx-auto px-4 space-y-12">
    <div class="text-center space-y-4 fade-in-up">
      <h2 class="text-4xl md:text-5xl font-bold">
        <span class="gradient-text">Awards</span>
      </h2>
      <p class="text-ink-muted max-w-2xl mx-auto">
        팀과 함께, 외부 심사로 인정받은 4건의 수상.
      </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
    {
      awards.map((award, i) => (
        <article
          class="glass-card rounded-2xl p-6 space-y-4 relative overflow-hidden fade-in-up break-keep"
          style={`animation-delay: ${0.1 * (i + 1)}s;`}
        >
          {/* 배경 하이라이트 */}
          <div
            class="absolute -top-10 -right-10 w-32 h-32 rounded-full blur-3xl pointer-events-none"
            style="background: radial-gradient(circle, rgba(255,255,255,0.06) 0%, transparent 70%);"
            aria-hidden="true"
          />

          {/* 헤더 — 모든 카드가 동일한 위치에서 시작하도록 등급명만 노출 */}
          <header class="relative">
            <h4 class="text-xl font-bold leading-tight whitespace-nowrap">
              <span class="gradient-text">{award.rank}</span>
            </h4>
          </header>

          {/* 본문 */}
          <div class="space-y-2 relative">
            <p class="text-sm font-medium text-ink leading-snug">{award.competition}</p>
            <p class="text-xs text-ink-muted">주관 · {award.organizer}</p>
            <p class="text-xs text-ink-muted leading-relaxed pt-1">{award.note}</p>
          </div>
        </article>
      ))
    }
    </div>
  </div>
</section>
